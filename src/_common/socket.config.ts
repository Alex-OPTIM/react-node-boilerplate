export enum UiSocketTopic {
    connect = "connect",
    disconnect = "disconnect",
    connection = "connection",
    iAmUi = "i-am-ui",
    checkTimeReq = "check-time-request",
    checkTimeRes = "check-time-response",
}
